export default function App() {
  return (
    <main className="app-shell">
      <section className="empty-state">
        <p className="eyebrow">Git Workscene</p>
        <h1>Local branches and worktrees, mapped.</h1>
        <p>
          The shell is ready. The next task wires real Git scanning into this
          canvas.
        </p>
      </section>
    </main>
  );
}
